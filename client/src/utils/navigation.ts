import { PRIMARY_NAV_ITEMS } from "../constants";
import type { NavItem } from "../types";
export const PRIMARY_NAV_ITEMS_TYPED: NavItem[] = PRIMARY_NAV_ITEMS;
export { PRIMARY_NAV_ITEMS_TYPED as PRIMARY_NAV_ITEMS };
